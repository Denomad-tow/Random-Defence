# CLAUDE.md — 친구들과 하는 랜덤 디펜스 웹게임

이 파일은 프로젝트의 설계서이자 작업 규칙이다. 작업을 시작할 때마다 이 파일을 먼저 읽고, 현재 진행 단계(로드맵)를 확인한 뒤 작업한다. 계획이 바뀌면 이 파일을 먼저 고친다.

---

## 1. 작업 규칙 (Claude Code가 반드시 지킬 것)

- 사용자는 **코딩 경험이 전혀 없다.** 설명은 쉬운 말로 하고, 전문 용어가 나오면 한 줄로 풀어서 설명한다.
- 실행, 설치, 확인 방법은 **복사해서 붙여넣을 수 있는 명령어와 함께 단계별로** 알려준다.
- **한 번에 한 단계만** 작업한다. 로드맵의 다음 단계로 넘어가기 전에 사용자에게 확인을 받는다.
- 단계가 끝나면 로드맵의 체크박스를 갱신하고, "무엇을 만들었는지 / 어떻게 확인하는지"를 짧게 정리해서 알려준다.
- 의미 있는 작업 단위마다 **git 커밋**을 한다. 문제가 생기면 이전 상태로 되돌리는 방법을 안내한다.
- **GitHub에 push(=실제 배포)는 사용자가 명확히 "배포해줘"라고 할 때만** 한다. 커밋은 평소처럼 계속하되, push는 따로 모아뒀다가 요청받았을 때 진행한다 (Vercel이 push마다 자동 배포하므로, 테스트 안 된 변경이 그대로 친구들에게 노출될 수 있음).
- 게임 수치(유닛 능력치, 확률, 비용 등)는 **코드에 직접 쓰지 않고 데이터 파일에서 읽는다.**
- 이 문서의 수치는 모두 **초안**이다. 플레이해보고 조절한다.
- 결정되지 않은 사항(11장)에 부딪히면 임의로 정하지 말고 사용자에게 선택지를 주고 묻는다.

---

## 2. 프로젝트 개요

- **장르:** 랜덤 디펜스 (웹 브라우저에서 플레이)
- **대상:** 친구 약 30명, 동시 접속 10명 이하
- **핵심 재미:** 랜덤 소환과 합성의 운 + 덱 구성과 배치의 전략 + 유닛을 모으고 키우는 성장
- **모드:** 개인전, 협동 파티전(2~5인), 경쟁 파티전(2~5인)
- **결제:** 실제 돈으로 결제하는 기능은 넣지 않는다.

---

## 3. 기술 스택 (초안)

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어 | TypeScript | 실수를 코드 작성 단계에서 잡아줌 |
| 개발 도구 | Vite | 설치와 실행이 간단함 |
| 게임 엔진 | Phaser 3 | 2D 웹게임에 적합, 게임 루프·드래그·애니메이션 지원 |
| 그래픽 | 아케인 룬 스타일, 코드로 직접 그림 (3-1장) | 그래픽도 재미의 일부, 90종을 일관되게 만들 수 있음 |
| 계정·저장·실시간 통신 | Supabase (4단계부터) | 로그인, 데이터 저장, 실시간 통신을 서버 개발 없이 해결 |
| 배포 | Vercel 또는 Netlify | 친구들에게 링크만 보내면 됨 |

실시간 파티전은 처음에 **방장 방식**(방장 브라우저가 몬스터 진행을 계산하고 다른 플레이어는 자기 행동만 보냄)으로 시도한다. 동기화 문제가 심하면 전용 게임 서버(예: Colyseus)로 바꾸는 것을 검토한다.

---

## 3-1. 그래픽과 연출 원칙

**그래픽은 나중으로 미루지 않는다.** 1단계 프로토타입부터 실제 게임처럼 보이고 느껴져야 한다. 도형이나 색 네모로 유닛을 대신하지 않는다.

### 아트 방향 — "아케인 룬" (결정됨)
- 콘셉트: **어두운 밤하늘 배경 위에 금빛 장식 테두리와 빛나는 보석 룬.** 고급스럽고 신비로운 분위기.
- 유닛은 캐릭터 그림 대신 **보석(육각형 결정) + 역할별 룬 문양**으로 표현한다. 역할은 문양으로, 등급은 보석 재질과 빛으로 구분한다.
- 몬스터는 어둠에서 나온 그림자 형체(검은 실루엣 + 빛나는 눈, 연기 같은 잔상)로 표현한다.
- 색: 배경은 거의 검은 남색, 장식과 UI 강조는 금색, 그 외 색은 등급 색에만 쓴다.
- 폰트: 제목·카드 이름은 한글 명조체 웹폰트(예: Noto Serif KR), 숫자와 작은 UI 글자는 읽기 쉬운 고딕체.
- 참고 미리보기: `style-preview.html` (등급 6종 카드와 역할 8종 룬 문양). 이 파일의 보석 모양, 재질 표현, 금테 장식을 기준으로 삼는다.

### 그래픽 제작 방법
- **모든 유닛·몬스터·UI 그래픽은 이미지 파일 없이 코드(SVG, Phaser 그래픽, 파티클)로 그린다.** 90종 유닛은 "보석 모양 × 등급 재질 × 역할 문양" 조합으로 자동 생성한다.
- 외부 에셋은 폰트와 효과음만 사용하고, 출처와 라이선스는 `assets/LICENSES.md`에 기록한다.
- 고급스러움을 위한 규칙: 색을 남발하지 않는다, 금색은 가늘고 절제해서 쓴다, 빛과 파티클은 높은 등급일수록 많아진다, 모든 움직임은 부드럽게(갑자기 나타나거나 사라지지 않게).

### 등급 시각 언어
등급은 멀리서 봐도 구분되어야 한다.

| 등급 | 색 (초안) | 연출 |
|---|---|---|
| 노말 | 회색 (돌) | 기본 테두리 |
| 고급 | 초록 (비취) | 테두리 |
| 희귀 | 파랑 (청옥) | 테두리 + 은은한 빛 |
| 영웅 | 보라 (자수정) | 빛 + 작은 반짝임 |
| 전설 | 황금 | 강한 빛 + 파티클 |
| 신화 | 진홍·무지개 | 회전하는 무지개 테두리 + 고유 파티클 |

별(★1~★7)은 유닛 위에 별 아이콘으로 표시하고, 별이 높을수록 유닛이 커지거나 빛나게 한다.

### 타격감과 연출 (1단계부터 적용)
- 공격마다 역할에 맞는 **발사체와 이펙트** (둔화는 얼음, 독은 초록 연기 등)
- 몬스터 피격 시 **하얗게 번쩍임**, 데미지 숫자 표시 (치명타는 크게)
- 몬스터 처치 시 터지는 파티클과 마나 획득 표시
- 소환 시 등장 애니메이션, **합성 시 빛이 모이는 연출**
- 보스 등장 시 경고 연출과 화면 흔들림
- 상자 열기는 **등급에 따라 연출이 달라진다.** 높은 등급일수록 기대감을 끌어올리고, 신화는 특별한 연출
- 버튼은 누를 때 눌리는 반응, 화면 전환은 부드럽게
- 한글이 예쁘게 나오는 웹폰트를 쓴다

### 성능
- 몬스터가 많아도 부드럽게(초당 60프레임 목표) 움직여야 한다. 이펙트가 많아져 느려지면 이펙트 수를 조절한다.

---

## 4. 폴더 구조 (초안)

```
/
├─ CLAUDE.md              ← 이 파일
├─ src/
│  ├─ core/               ← 게임 판 로직 (화면과 분리, 모든 모드에서 재사용)
│  │  ├─ board.ts         ← 칸, 배치, 합성
│  │  ├─ wave.ts          ← 웨이브, 몬스터 이동, 보스
│  │  ├─ combat.ts        ← 공격, 특성 효과 처리
│  │  └─ economy.ts       ← 마나(인게임 재화), 소환 비용
│  ├─ meta/               ← 게임 밖 시스템 (컬렉션, 덱, 뽑기, 레벨업)
│  ├─ modes/              ← 개인전, 협동, 경쟁
│  ├─ scenes/             ← Phaser 화면 (메인, 덱 편집, 게임, 결과, 상자)
│  ├─ data/               ← 수치 데이터 파일
│     ├─ units.json       ← 90종 유닛
│     ├─ rarities.json    ← 등급별 배율
│     ├─ waves.json       ← 웨이브·몬스터·보스
│     └─ gacha.json       ← 상자 종류, 확률, 천장
├─ assets/                ← 폰트, 효과음
│  └─ LICENSES.md         ← 에셋 출처와 라이선스
└─ ...
```

핵심 원칙: **`core`는 화면(Phaser)을 모른다.** 같은 판 로직을 개인전, 협동, 경쟁에서 그대로 쓰기 위해서다.

---

## 5. 게임 판 규칙 (인게임)

### 재화 이름
- **마나:** 게임 한 판 안에서만 쓰는 재화 (소환, 인게임 강화)
- **골드:** 게임 밖에서 쓰는 재화 (유닛 레벨업)

### 판 구성
- 플레이어 필드는 **3행 × 5열 = 15칸**
- 몬스터는 필드 바깥의 정해진 길을 따라 이동
- 필드에 살아있는 몬스터가 **100마리 이상**이 되면 패배

### 소환
- 마나를 써서 **덱 안의 5종 중 랜덤 유닛 1개**를 빈 칸에 ★1로 소환
- 소환 비용은 10마나에서 시작, 소환할 때마다 10씩 증가
- 몬스터를 잡으면 마나 획득

### 합성
- **같은 유닛 + 같은 별** 2개를 드래그로 합치면 → **별이 1 오른 덱 안의 랜덤 유닛** 1개
- 별은 ★1 ~ ★7. 별이 오를수록 공격력이 크게 오른다.
- 카드 **등급**(노말~신화)과 합성 단계인 **별**은 다른 개념이다. 용어를 섞어 쓰지 않는다.

### 웨이브
- 웨이브마다 몬스터 수와 체력이 증가
- **10웨이브마다 보스.** 제한 시간 안에 못 잡으면 패배
- 도달한 최고 웨이브를 기록

### 인게임 강화
- 마나를 써서 **유닛 종류별**로 그 판에서만 적용되는 강화 (1~5단계)
- 강화 비용은 단계마다 증가

---

## 6. 유닛 시스템

### 역할 15가지

| 역할 | 기본 특성 | 주로 유리한 상황 |
|---|---|---|
| 단일 딜러 | 한 대상에게 강한 공격 | 보스 |
| 광역 | 범위 안의 여러 대상 공격 | 일반 웨이브 |
| 둔화 | 이동속도 감소 | 몬스터가 몰릴 때 |
| 기절 | 확률로 잠깐 멈춤 | 보스 시간 벌기 |
| 독 | 시간에 걸쳐 지속 피해 | 체력 높은 몬스터 |
| 방어력 감소 | 대상이 받는 피해 증가 | 다른 딜러와 조합 |
| 버프 | 주변 유닛 공격속도 증가 | 딜러 옆에 배치 |
| 골드 생성 | 공격 대신 주기적으로 마나 생성 | 초반 경제 |
| 관통 | 같은 줄에 있는 몬스터를 한 번에 여러 마리 공격 | 몬스터가 줄지어 내려올 때 |
| 다중 사격 | 서로 다른 대상 2마리를 동시에 공격 | 여러 목표 동시 견제 |
| 사슬 번개 | 맞은 대상에서 근처 대상으로 피해가 튕겨나감(감쇠) | 몬스터가 뭉쳐있을 때 |
| 처형 | 체력이 낮은 대상에게 추가 피해(확인사살) | 마무리 |
| 마나 흡수 | 공격 적중 시 확률로 마나 추가 획득 | 경제 보완 |
| 냉기 결계 | 공격하지 않고 사거리 안 대상을 계속 둔화(항상 켜짐) | 몰이 지연 |
| 치명 강타 | 낮은 확률로 매우 센 치명타 | 폭딜 |

(역할 이름 "골드 생성"은 인게임에서는 **마나**를 만든다.)

### 등급 6가지

노말, 고급, 희귀, 영웅, 전설, 신화

- **모든 등급에 15가지 역할의 유닛이 1종씩** 있다 → 총 **90종**
- 등급이 높을수록 기본 능력치가 높고, 특성 효과가 강해지거나 추가 효과가 붙는다.

| 등급 | 능력치 배율 (초안) | 특성 |
|---|---|---|
| 노말 | 1.0 | 기본 효과 |
| 고급 | 1.3 | 기본 효과 강화 |
| 희귀 | 1.7 | 적용 대상 증가 |
| 영웅 | 2.2 | 추가 효과 1개 |
| 전설 | 2.9 | 추가 효과 강화 |
| 신화 | 3.8 | 고유 효과 |

예시 (둔화 역할):
노말 20% 감소 → 고급 30% → 희귀 30%, 2마리 동시 → 영웅 40%, 광역 → 전설 40%, 광역 + 방어력 감소 → 신화 50%, 광역 + 확률로 완전 정지

### 유닛 데이터 형식 (units.json 초안)

```json
{
  "id": "slow_normal",
  "name": "서리 견습생",
  "rarity": "normal",
  "role": "slow",
  "attack": 10,
  "attackSpeed": 1.0,
  "range": 2,
  "effects": [{ "type": "slow", "value": 0.2, "duration": 1.5 }]
}
```

유닛 이름과 세부 수치는 2단계에서 90종을 채우면서 정한다.

---

## 7. 덱과 성장 (게임 밖)

### 덱
- 보유한 유닛 카드 중 **5종**을 골라 덱을 짠다. 등급이 섞여도 된다.
- 게임 중 소환과 합성은 덱 안의 5종에서만 나온다.
- 덱은 여러 개 저장해둘 수 있다 (초안: 3개).

### 영구 레벨업
- 유닛마다 레벨이 있다 (초안: 1~15).
- 레벨업에 필요한 것: **중복 카드 + 골드**. 레벨이 오를수록 필요량 증가.
- 레벨이 오르면 해당 유닛의 기본 능력치가 오른다.

### 판 종료 보상
- 골드 + 상자. 도달 웨이브가 높을수록 보상과 상자 등급이 좋아진다.

---

## 8. 뽑기 (상자)

- 상자를 열면 **랜덤 유닛 카드**를 얻는다.
- 기본 확률 (초안, gacha.json에서 조절):

| 등급 | 확률 |
|---|---|
| 노말 | 50% |
| 고급 | 28% |
| 희귀 | 14% |
| 영웅 | 6% |
| 전설 | 1.7% |
| 신화 | 0.3% |

- 등급이 정해지면 그 등급의 8종 중 하나가 균등 확률로 나온다.
- **천장:** 신화가 일정 횟수(초안: 카드 200장) 동안 안 나오면 다음 카드는 신화 확정. 천장 카운트는 화면에 표시한다.
- 상자 종류는 여러 개 (초안: 나무, 은, 금). 좋은 상자일수록 카드 수가 많고 높은 등급 확률이 오른다.
- 획득 확률은 게임 안에서 볼 수 있게 표시한다.
- 중복 카드는 레벨업 재료가 된다.

---

## 9. 모드

### 개인전
- 혼자 웨이브를 막으며 최고 웨이브에 도전
- 모든 성장 시스템의 기본 보상원

### 협동 파티전 (2/3/4/5인 선택)
- 실시간. 각자 자기 필드에 유닛을 배치하고, **같은 길로 오는 몬스터를 함께 막는다.**
- 인원이 많을수록 몬스터 수와 체력 증가, 보상도 증가
- 한 명의 필드가 무너지면 전원 패배할지, 남은 사람이 이어갈지는 11장에서 결정

### 경쟁 파티전 (2/3/4/5인 선택)
- 실시간. 각자 자기 필드를 방어한다.
- 몬스터를 잡으면 일정 비율로 **다른 플레이어 필드에 몬스터를 보낸다.**
- 마지막까지 버틴 사람이 승리
- **같은 인원끼리만** 매칭, 순위도 2인·3인·4인·5인 별로 따로
- 시즌제 순위

---

## 10. 로드맵

각 단계는 사용자가 "N단계 시작해줘"라고 하면 시작한다. 완료 기준을 모두 만족하면 체크한다.

### 0단계 — 개발 환경 준비
- [x] Node.js, Git 설치 안내 (사용자 컴퓨터 환경에 맞게) — 이미 설치돼 있어 안내 생략
- [x] Vite + TypeScript + Phaser 프로젝트 생성
- [x] 빈 화면이 브라우저에 뜨는 것 확인
- [x] style-preview.html을 참고해 공통 그래픽 모듈(보석, 금테, 룬 문양, 등급 재질) 만들기
- [x] 첫 git 커밋

### 1단계 — 개인전 프로토타입 (노말 8종)
- [x] 15칸 필드와 몬스터 이동 길
- [x] 웨이브 진행, 10웨이브마다 보스
- [x] 노말 등급 8종 유닛 (아케인 룬 그래픽)
- [x] 밤하늘 배경, 금테 장식 길, 필드 칸, 그림자 몬스터
- [x] 3-1장의 타격감과 연출
- [x] 덱 5종 선택 화면 (간단하게)
- [x] 마나 소환, 드래그 합성, ★1~★7
- [x] 15가지 특성 효과 동작
- [x] 인게임 강화
- [x] 패배 조건, 최고 기록 저장 (브라우저 저장)
- **완료 기준:** 10분 이상 플레이했을 때 "한 판 더"가 떠오르고, 화면만 봐도 "게임 같다"는 느낌이 드는지 사용자가 확인

### 2단계 — 유닛 90종 완성
- [x] 6등급 × 15역할 데이터 작성 (이름, 수치, 등급별 추가 효과) — 90종 전부 작성. 영웅=추가 효과 1개, 전설=추가 효과 강화, 신화=역할 간 효과를 조합한 고유 효과
- [x] 90종 유닛 그래픽 (보석 × 재질 × 문양 조합) — 룬 문양 15개 × 등급별 보석 재질 자동 조합, 90종 전부 확인
- [x] 등급 시각 언어 (3-1장: 테두리, 빛, 파티클) — 0단계에서 6등급 전부 준비됨 (테두리/빛/반짝임 개수, 신화는 무지개 테두리)
- [x] 등급별 추가 효과의 이펙트 — 공격 발사체가 등급이 높을수록 커지고 빛나며, 희귀 이상은 잔상 스파크가 붙음
- [x] 테스트용으로 아무 유닛이나 덱에 넣을 수 있는 개발자 모드 — 덱 선택 화면의 "[개발자] 전체 획득" 버튼
- **완료 기준:** 등급 간 차이가 체감되고, 특정 유닛 하나가 압도적이지 않음 — 수치는 초안이라 실제 플레이하며 조정 필요

### 3단계 — 성장 시스템 (아직 브라우저 저장)
- [x] 보유 카드 컬렉션 화면 — 레벨/보유수 확인 + 레벨업 (페이지 넘김)
- [x] 상자 열기 (등급별로 다른 연출), 확률, 천장 — 나무/은/금 상자, 높은 등급일수록 반짝임 연출 큼, 신화 200장 천장
- [x] 판 종료 보상 (골드, 상자) — 패배 시 도달 스테이지에 비례해 골드+상자 지급
- [x] 유닛 레벨업 (중복 카드 + 골드) — 1~15레벨, 레벨마다 비용 증가, 영구 능력치 상승
- [x] 덱 편집 화면 (여러 덱 저장) — 덱 3개 슬롯, 탭으로 전환
- **완료 기준:** 처음 시작한 계정으로 30분 플레이했을 때 성장하는 느낌이 있음 — 실제 친구 테스트로 확인 필요

### 4단계 — 계정과 배포
- [x] Supabase 가입과 설정 안내 — 프로젝트 `random-defence` 생성, "Confirm email" 끔 (가짜 이메일 닉네임 가입 방식이라 필수)
- [x] 로그인 (친구들이 따로 가입하지 않아도 되는 방식 우선) — 닉네임+비밀번호만으로 가입/로그인, 실제 이메일 불필요. 세션은 브라우저에 자동 저장되어 다음 접속 때 로그인 화면 건너뜀
- [x] ~~초대 코드로 친구만 입장~~ — 지인들에게만 링크를 전달하는 방식이라 필요 없다고 판단, 생략하기로 결정 (닉네임 회원가입만으로 진행)
- [x] 컬렉션·레벨·기록을 서버에 저장 — Supabase `player_data` 표(로그인한 사람만 자기 데이터 읽기/쓰기 가능)에 브라우저 저장 내용을 통째로 동기화. 로그인 시 서버→브라우저로 불러오고, 20초마다·탭을 떠날 때·로그아웃 직전에 브라우저→서버로 저장
- [x] ~~상자 열기와 보상 계산을 서버 쪽에서 처리~~ — 지인들끼리 하는 게임이라 클라이언트 조작 위험을 지금은 감수하기로 하고 건너뜀 (필요해지면 나중에 다시 진행)
- [x] 인터넷에 배포, 친구 몇 명에게 테스트 — GitHub(`Denomad-tow/Random-Defence`, main 브랜치) + Vercel 연동. 프로덕션 주소: `https://random-defence.vercel.app`
- **완료 기준:** 다른 기기에서 로그인해도 내 데이터가 그대로 보임 — 로컬 테스트로 확인 완료. 실제 친구 테스트는 아직

### 5단계 — 협동 파티전
- [x] 방 만들기 / 참가 (2~5인 선택) — 방 코드로 만들기/참가, 대기실에서 실시간으로 인원 목록 확인, 방 만들 때 인원(2/3/4/5인) 선택, 정원 차면 늦게 들어온 사람은 자동으로 방에서 나가지고 안내 메시지가 뜨도록 완료
- [ ] 실시간 동기화 (방장 방식) — 4/4 완료: 몬스터 위치·체력 실시간 전달, 소환·공격이 공유 체력을 깎는 것, 몬스터 처치 보상(마나)·보스 등장 연출·필드 뚫림 시 전투 종료와 보상(골드·상자) 분배에 이어, 방장 접속이 끊기면 파티원 쪽에서 전투를 자동 종료하고 보상을 받도록 처리. 합성·강화·직업별 특수효과는 아직 연결 안 됨(별도 단계에서 진행 예정)
- [x] 인원별 난이도·보상 조절 — 인원이 늘수록 몬스터 체력 증가(1명 늘 때마다 +50%, `core/coopBalance.ts`에서 쉽게 조정 가능). 보상은 도달 스테이지 기준 그대로라 더 멀리 갈수록 자연히 더 받는 구조 — 별도 인원 배율은 두지 않음. 화면 상단에 현재 체력 배율 표시
- [x] 접속이 끊긴 플레이어 처리 — 방장이 끊기면(5초 유예 후에도 목록에서 계속 안 보이면) 파티원 화면이 자동으로 전투를 종료하고 그 시점 스테이지 기준 보상을 받는다. 파티원이 끊기는 경우는 인원 목록에서만 자연스럽게 사라지고, 방장 쪽 전투는 계속 진행됨(별도 처리 불필요)
- **완료 기준:** 친구 2명 이상과 끝까지 문제없이 한 판 완료

### 6단계 — 경쟁 파티전
- [x] 같은 인원끼리 매칭 — 실제 매칭 큐 대신, 협동전과 같은 "방 코드" 방식을 재사용(친구끼리 하는 용도라 이게 더 단순하고 자연스럽다고 판단). 방 만들 때 "경쟁전(일반)"/"경쟁전(균형)" 모드를 고름
- [x] 몬스터 보내기 — 몬스터를 잡을 때마다 20% 확률로 살아있는 다른 플레이어 중 무작위 한 명에게 몬스터 하나를 보냄 (`core/versusBalance.ts`에서 확률 조정 가능)
- [x] 인원별 순위 — "순위" 화면 추가(덱 선택 화면 하단 메뉴). 2/3/4/5인 탭별로 역대 승리 횟수 순위를 보여줌. 시즌제(주기적 초기화)는 규모가 커서 이번엔 빼고, 역대 전적만 우선 기록
- [x] 레벨 차이 보정 규칙 — 일반전(그대로 적용)과 균형전(모두 일반 등급 유닛만 사용) 두 모드를 다 만듦. 방 만들 때 선택
- **완료 기준:** 5인전이 끝까지 문제없이 진행 — 2인 기준으로 직접 확인 완료, 5인 실제 테스트는 아직

### 7단계 — 다듬기
- [ ] 효과음과 배경음악 — 실제 음원 파일이 필요해서 보류 중. 무료 효과음 사이트(Kenney.nl, freesound.org 등)에서 파일을 받아주시면 코드로 연결 가능
- [x] 그래픽 세부 다듬기 — 소환 애니메이션·합성 시 빛 모이는 연출·역할별 색 발사체·보스 등장 연출 등은 이미 되어 있던 것을 확인. 이번에 추가한 것: 소환 버튼을 누를 때 살짝 눌리는 반응(솔로·협동·경쟁 3곳 모두), 신화 등급 상자 개봉 시 화면 흔들림 + 무지개색 빛 번쩍임 연출
- [x] 튜토리얼 — 처음 접속한 사람에게만(로컬 저장 플래그) 솔로 전투 화면에서 5단계 사용법 안내가 한 번 뜸(환영 인사 → 소환 → 합성 → 강화 → 계속 성장하기). "건너뛰기"로 언제든 닫을 수 있음
- [ ] 밸런스 조정 (친구들 피드백 반영) — 아직 실제 친구 테스트를 안 해봐서 반영할 피드백이 없음. 친구 테스트 이후 진행

---

## 11. 결정이 필요한 사항

- [x] 협동 파티전의 몬스터 사냥 방식: 각자 따로 잡기 / **몬스터 체력을 진짜로 공유**(다같이 때려서 깎기)로 결정
- [x] (위 결정 때문에 재검토) 몬스터가 너무 쌓여서 필드가 뚫리면: 전원 패배 / 남은 사람이 이어감 / **파티 전체가 함께 종료**로 최종 결정 — 몬스터 길과 체력이 파티 전체가 공유하는 하나뿐이라, "나만 뚫리는" 상황 자체가 거의 없어서 개인별로 나누는 게 부자연스럽다고 판단
- [x] 경쟁 파티전의 레벨 차이: **모드를 2개 다 만들기**로 결정 — ① 일반전(그대로 적용, 지금까지 키운 대로 승부) ② 균형전(차이를 줄이는 보정 적용). 방 만들 때 둘 중 하나를 고른다
- [x] 아트 스타일: 아케인 룬 (3-1장)
- [ ] 유닛 90종 이름 (아케인 룬 분위기에 맞춰, 2단계에서 결정)
- [ ] 모바일에서도 플레이할지 (화면 크기와 터치 조작에 영향)
- [ ] 천장 횟수, 상자 종류별 확률

---

## 12. 변경 기록

- 초안 작성: 랜덤 디펜스, 3가지 모드(개인전, 협동·경쟁 파티전 2~5인), 6등급 × 8역할 48종 유닛, 상자 뽑기와 천장, 영구 레벨업
- 그래픽 방침 변경: 도형 임시 그래픽 대신 처음부터 완성도 있는 그래픽과 타격감 연출 적용 (3-1장 추가)
- 아트 스타일 확정: 아케인 룬 (어두운 배경, 금테, 보석 룬 유닛, 그림자 몬스터), 모든 그래픽을 코드로 제작, style-preview.html 추가
- 역할 8가지 → 15가지로 확장 (관통/다중 사격/사슬 번개/처형/마나 흡수/냉기 결계/치명 강타 추가), 등급당 유닛 수 48종 → 90종으로 변경
- 덱을 매번 고르지 않고 브라우저에 저장해서 재사용, "덱 변경" 화면에서 언제든 교체 가능하도록 변경 (나중에 상자로 상위 등급을 얻었을 때 교체하는 흐름의 기반)
- 유닛 공격 사거리를 화면에서 원으로 볼 수 있는 토글 버튼 추가
- 몬스터 길을 단일 고정 경로에서 "칸 사이 틈으로 지나가는 맵 12종" 중 판마다 무작위로 고르는 방식으로 변경 (슬롯 15칸 배치는 그대로 유지)
- "연구" 화면 추가: 골드로 공격력/공격속도(전체 공통)와 역할별 특성(15종)을 각각 최대 10레벨까지 영구 강화
- 4단계 시작: Supabase 프로젝트 연결 + 닉네임/비밀번호 로그인 화면(가짜 이메일 방식, 실제 이메일 불필요) 추가
- 초대 코드 기능은 생략하기로 결정 (지인들에게만 링크 공유, 닉네임 가입만으로 충분)
- 게임 데이터(골드·컬렉션·덱·레벨·상자·연구·최고 기록)를 Supabase에 동기화해 다른 기기에서 로그인해도 이어서 할 수 있게 함
- 상자 열기 서버 처리는 생략하고 4단계 완료: GitHub(Denomad-tow/Random-Defence) + Vercel로 배포, https://random-defence.vercel.app 에서 실제 접속 확인
- 유닛 강화 확인 창 추가 + 탭이 드래그로 오인식되어 씹히던 버그 수정 (Phaser 드래그 최소 거리 설정)
- 우편함 + 운영자 우편 발송 기능 추가: "관리" 계정을 운영자로 지정해 공지/이벤트 보상(골드·상자)을 전체 또는 특정 닉네임에게 발송 가능. Supabase RLS/함수로 운영자가 아니면 서버에서 발송이 거부되도록 처리
- 게임 중 나가기 버튼, 몬스터 실루엣 모양 6종(유령/박쥐/늑대/골렘/거미/점액, 능력치는 그대로) 무작위 등장, 덱 선택 화면 상단 버튼 70% 축소, 로그인한 닉네임을 덱 선택·인게임 화면에 표시, 운영자 우편 발송 시 특정 닉네임을 목록에서 선택하도록 변경
- 유닛 합성 결과가 등급과 무관하게 덱 전체에서 무작위로 나오던 버그 수정: 이제 합쳐진 두 유닛과 같은 등급 안에서만 무작위로 결과가 나옴
- 게임 중 "나가기" 버튼에 확인/취소 팝업 추가, 유닛 소환 직후 강화 확인창이 잘못 뜨던 버그 수정 (같은 유닛을 짧게 두 번 탭해야만 강화 확인창이 뜨도록 변경)
- 덱 선택·인게임 화면 닉네임 글씨 크기를 나가기 버튼과 동일하게 확대, 연구 최대 레벨 10→30, 인게임 배속(1x/2x/4x/8x) 기능 추가, 연구 화면 글씨 크기를 컬렉션 화면과 동일한 방식으로 통일
- 금 상자보다 높은 등급 "다이아 상자" 추가 (스테이지 100 이상), 금 상자 획득 조건 40~99로 변경, 게임 화면 "덱 변경" 버튼 제거
- "도감" 화면 추가: 등급별 탭으로 15개 역할 유닛의 분류(공격형/제어형/버프형/자원형)·특성 설명·공격력/공격속도/사거리를 확인 가능. 기존 능력치 데이터를 점검해보니 역할·등급별로 이미 의미 있게 차이 나 있어 별도 수치 조정은 하지 않음
- 도감 화면 카드 레이아웃 수정: 잘려 보이던 유닛 아이콘을 컬렉션 화면과 같은 크기로 왼쪽에 배치, 가운데에 이름·분류·특성을 더 큰 글씨로, 오른쪽에 공격/속도/사거리를 세 줄로 나눠 표시
- 도감 화면 글씨 크기 재조정: 상단 등급 탭 글씨 1.2배 확대, 가운데 이름·분류·특성 글씨를 컬렉션 화면 특성 설명 크기로 축소(줄 간격도 좁힘), 오른쪽 공격/속도/사거리 글씨를 절반 크기로 축소(줄 간격도 좁힘)
- 도감 화면 가운데 이름·분류·특성 텍스트를 카드 세로 중앙에 오도록 정렬 (기존엔 위쪽에 붙어 있었음)
- 5단계(협동 파티전) 시작: "파티" 대기실 화면 추가. 방 코드로 방 만들기/참가, 실시간으로 누가 들어왔는지 확인, 방장만 "게임 시작" 가능. 별도 서버 표 없이 Supabase Realtime 프레즌스만으로 구현 (무료 요금제로 충분). 실제 협동 전투 연결은 다음 단계
- 협동 파티전 설계 결정: 필드가 뚫려도 남은 사람이 이어감, 몬스터 체력은 파티원 전체가 진짜로 공유(다같이 때려서 깎기)
- 협동 전투 화면(CoopGameScene) 추가, 실시간 동기화 1/4 완료: "게임 시작"을 누르면 방장·파티원 모두 협동 전투 화면으로 이동. 방장 기기가 몬스터 스폰·이동을 계산해서 초당 여러 번 위치·체력을 파티원에게 전달하고, 파티원은 받은 대로 그대로 그린다 (아직 유닛 배치·공격은 연결 안 됨, "보기 전용" 단계). 두 브라우저로 실제 테스트해서 같은 몬스터가 같은 자리에 똑같이 보이는 것을 확인함
- 파티원 화면 몬스터 이동이 끊겨 보이던 문제 수정: 위치 정보를 초당 6~7번만 받다 보니 그 사이엔 멈춰 있다가 순간이동하듯 튀었는데, 받은 두 위치 사이를 매 프레임 부드럽게 이어서 그리도록(보간) 고침
- 협동 전투에 소환·공격 연결, 실시간 동기화 2/4 완료: 협동 전투 화면에 15칸 필드와 "소환" 버튼 추가(합성·강화는 다음에, 이번엔 소환만). 소환하면 몬스터 길에서 가장 가까운 빈 칸에 자동 배치(칸을 직접 고르는 건 아직 없음). 배치한 유닛이 사거리 안 몬스터를 자동 공격하고, 그 피해가 방장 기기에 모여서 진짜 공유 체력을 깎음 — 내가 때리든 파티원이 때리든 같은 몬스터 체력이 줄고, 죽으면 모두의 화면에서 사라짐. 두 브라우저로 실제 공격→공유 체력 감소→처치까지 확인함
- 협동 전투 버그 2건 수정: (1) 파티원 화면이 여전히 끊겨 보이던 문제 — 위치 보간 구간을 고정 150ms 대신 "직전에 실제로 걸린 시간"에 맞춰 늘였다 줄였다 하도록 바꾸고, 기준 시계도 Phaser 게임 루프 대신 실제 시계(Date.now)로 바꿔서 인터넷 상태가 들쭉날쭉해도 덜 끊기게 함. (2) 몬스터를 잡아도 마나가 안 쌓이던 문제 — 애초에 처치 보상(마나 획득) 로직 자체가 빠져 있었음. 몬스터가 죽으면 방장이 자기 마나를 올리고 파티원 전원에게도 "몬스터 잡았다" 신호를 보내 같은 만큼 마나가 오르도록 추가
- 협동 전투 종료·보상 분배, 실시간 동기화 3/4 완료: 끝까지 도달한 몬스터가 이제 사라지지 않고 필드 끝에 계속 쌓인다(솔로 모드와 동일). 100마리까지 쌓이면(필드 뚫림) 방장이 판단해 파티 전체에 "전투 종료" 신호를 보내고, 모두가 같은 스테이지 기준으로 골드·상자 보상을 받는다("협동 전투 종료" 화면 표시). 보스 몬스터 등장 시 화면 흔들림 + "보스 출현!" 연출도 추가. 설계 결정: 몬스터 길·체력이 파티 전체가 공유하는 하나뿐이라, 필드 뚫림도 개인별로 나누지 않고 파티 전체가 함께 종료하는 것으로 최종 정리 (11장 참고)
- 협동 전투 접속 끊김 처리, 실시간 동기화 4/4 완료(마지막 조각): 방장 기기만 몬스터를 계산하는 구조라, 방장이 튕기면 파티원 화면이 그대로 멈춰버리는 문제가 있었음. 파티원 쪽에서 인원 목록에 방장이 계속 안 보이면(순간적인 끊김과 구분하려고 5초 유예) "방장 연결이 끊겨서 전투가 끝났어요" 화면으로 자동 종료하고 그 시점 스테이지 기준 보상을 받도록 처리. 파티원이 끊기는 경우는 방장 쪽 전투에 영향 없음(인원 목록에서만 자연스럽게 사라짐). 실제로 방장 창을 강제로 닫아서 파티원 화면이 정상 종료되는 것을 확인함
- 협동 전투 화면 "나가기" 버튼에 솔로 모드와 동일한 확인/취소 팝업 추가 (실수로 눌러서 바로 방을 나가버리는 일이 없도록)
- 5단계(협동 파티전) "인원별 난이도·보상 조절" 완료: 파티 인원이 늘어날수록 몬스터 체력이 세짐(1명 늘 때마다 +50%, 처음부터 정해둔 숫자가 아니라 `core/coopBalance.ts` 한 곳에서 쉽게 조정 가능하게 만듦). 보상은 기존처럼 도달 스테이지 기준으로만 계산 — 더 멀리 갈수록 자연히 더 받으니 별도 인원 배율은 두지 않음. 화면 상단 "함께:" 줄에 현재 체력 배율(예: x1.5)도 같이 보여줌
- 5단계 마지막 항목 "방 만들기/참가 인원 제한(2~5인)" 완료: 방 만들 때 2/3/4/5인 중 정원을 고를 수 있고, 대기실에 "모인 인원: N/M명"으로 표시됨. 정원이 찬 뒤 늦게 들어오려는 사람은 서버 없이 클라이언트끼리 판단해서 자동으로 방에서 나가지고 "방이 꽉 찼어요" 안내를 봄(서버가 없는 구조라 완벽히 막지는 못하지만 친구끼리 쓰는 용도로는 충분)
- 협동 전투에 솔로 모드와 동일한 배속(1x/2x/4x/8x) 기능 추가: 실제 몬스터 계산은 방장만 하므로 배속 버튼도 방장에게만 보이고, 방장이 바꾸면 파티원 전원에게 전달돼 파티원 화면의 유닛 공격 속도도 같이 맞춰짐(몬스터 이동 자체는 방장이 보내주는 값을 그대로 따라가서 이미 저절로 빨라짐). 파티원 화면에는 "배속 Nx (방장 설정)" 읽기 전용 표시. 이로써 5단계(협동 파티전) 로드맵 4개 항목 모두 완료 — 남은 건 실제 친구들과의 플레이 테스트
- 6단계(경쟁 파티전) 추가: 파티 화면에 "협동전/경쟁전(일반)/경쟁전(균형)" 모드 선택 추가(방 만들 때 고름, 대기실에 모드 표시). 협동전과 달리 몬스터 길·체력을 공유하지 않고, 각자 자기 필드에서 솔로 모드처럼 독립적으로 몬스터를 잡는 새 전투 화면(VersusGameScene)을 만듦 — 그래서 방장이 계산을 도맡을 필요가 없어져 오히려 구조가 더 단순해짐. 몬스터를 잡으면 20% 확률로 살아있는 다른 사람에게 몬스터 하나를 보내고(선물 받은 몬스터는 보라색으로 표시), 내 필드가 뚫리면(몬스터 100마리) 그 시점 순위로 탈락, 마지막까지 남은 사람이 승리. 균형전 모드는 모두 일반 등급 유닛만 쓰게 해서 그동안 키운 격차를 줄임. 순위표·시즌제는 이번엔 빼고 이번 판 결과(승리/몇 등 탈락)만 표시. 두 브라우저로 독립 스폰·몬스터 선물·탈락→승리 판정까지 실제로 확인함
- 6단계 마지막 항목 "인원별 순위" 완료: 경쟁전 한 판이 끝날 때마다 결과(모드·인원수·등수·도달 스테이지)를 Supabase `versus_matches` 표에 기록하고, 새 "순위" 화면(덱 선택 하단 메뉴)에서 2/3/4/5인 탭별로 역대 승리 횟수 순위를 볼 수 있음. 본인 닉네임으로만 결과를 기록할 수 있도록 RLS로 막아 가짜 승리 기록을 못 남기게 함. 시즌제(주기적 초기화)는 규모가 커서 다음에 따로 진행하기로 함
- 7단계(다듬기) 튜토리얼 + 그래픽 디테일 진행: 처음 접속한 사람에게만 솔로 전투 화면에서 5단계짜리 짧은 사용법 안내(환영 → 소환 → 합성 → 강화 → 계속 성장하기)가 한 번 뜨고, "건너뛰기"로 언제든 닫을 수 있음(로컬 저장 플래그로 재접속 시 다시 안 뜸). 소환 버튼에 누를 때 살짝 눌리는 반응 추가(솔로·협동·경쟁 3개 화면 모두). 신화 등급 상자를 열면 화면이 흔들리고 무지개색 빛이 번쩍이는 특별 연출 추가. 효과음/배경음악은 실제 음원 파일이 있어야 해서 보류, 밸런스 조정은 친구 테스트 이후로 미룸
- 중요 버그 수정: PC에서는 멀쩡한데 휴대폰으로 접속하면 컬렉션·연구·도감·덱 선택 화면 글씨가 서로 겹쳐서 깨져 보이던 문제. 원인은 화면 배율(휴대폰은 화면이 작아도 화면 속 점(픽셀)을 2~3배 촘촘하게 쓰는 경우가 많은데, 이 배율값을 글씨 크기 계산에 실수로 두 번 곱해서 휴대폰에서만 글씨가 2~3배 커져버렸던 것) — PC 테스트 화면은 이 배율이 항상 1이라 문제가 전혀 드러나지 않았음. 4개 화면에서 잘못 곱해진 부분을 모두 찾아 제거해서 수정. 앞으로는 실제 휴대폰과 비슷한 화면 배율로도 테스트를 병행하기로 함
- 휴대폰 글씨 크기 추가 조정: 덱 선택 화면 상단 메뉴 버튼(연구/상자/컬렉션/도감/우편함/파티/순위/로그아웃) 글씨를 저장 버튼과 같은 크기로 확대. 도감 화면의 유닛 중앙 설명(이름/분류/특성)과 우측 설명(공격/속도/사거리) 글씨를 2배로 확대. 연구·컬렉션 화면의 이름/특성/레벨 설명 글씨도 도감과 같은 비율로 2배 확대(레벨업·연구 버튼 자체 글씨 크기는 그대로 유지)
- 7일 출석 체크 추가: 처음 가입한 날을 1일차로 시작해 접속할 때마다 하루에 한 번 보상을 받을 수 있음(1일 은 상자 1개 → 2일 은 상자 2개 → 3일 금 상자 1개 → 4일 은 상자 3개 → 5일 은 상자 4개 → 6일 금 상자 2개 → 7일 다이아 상자 1개, 7일차를 받으면 다시 1일차부터 반복). 보상표는 `core/attendanceBalance.ts` 한 곳에서 쉽게 바꿀 수 있음. 덱 선택 화면에 들어갈 때 오늘 받을 보상이 남아있으면 자동으로 팝업이 뜨고, "받기" 없이 닫아도 그날 안에 다시 들어오면 계속 알려줌(연속 출석이 끊겨도 벌칙 없이 이어서 진행). 다른 게임 데이터처럼 계정에 동기화되어 다른 기기에서 로그인해도 이어짐
- 연구 버튼의 "골드N" 비용 글씨와 컬렉션 레벨업 버튼의 "중복N·골드N" 비용 글씨가 배경과 비슷한 색이라 잘 안 보이던 문제 수정: 글씨를 1.5배 키우고, 돈이 충분할 때는 밝은 크림색, 부족할 때도 이전보다 밝은 색으로 표시
- 회원 탈퇴 기능 추가: 덱 선택 화면의 로그아웃 버튼 아래 작은 "회원 탈퇴" 링크를 누르면 안내와 함께 비밀번호 재입력 창이 뜨고, 비밀번호가 맞아야 탈퇴가 진행됨(재로그인 방식으로 본인 확인). 탈퇴하면 계정과 서버에 저장된 데이터가 완전히 삭제되며 같은 닉네임으로 다시 가입할 수 있음. Supabase에 본인 계정만 삭제할 수 있는 서버 함수(`delete_own_account`)를 새로 추가함
- 휴대폰 "홈 화면에 추가"로 앱처럼 실행하는 기능 추가: 게임 전용 아이콘(금색 "랜" 글자, 192px/512px) 제작, `public/manifest.webmanifest` 추가, index.html에 관련 설정을 넣어 홈 화면에 추가하면 주소창 없이 전체 화면 앱처럼 열리게 함. 화면을 확대/축소하는 핀치 줌도 꺼서(게임 조작 중 실수로 확대되는 것 방지) 더 앱같이 느껴지게 함
- 협동전·경쟁전 파티 전투 중 채팅 기능 추가: 화면 오른쪽 위 말풍선 버튼을 누르면 채팅창이 열리고, 파티원끼리 실시간으로 메시지를 주고받을 수 있음. 채팅창을 닫아둔 사이 새 메시지가 오면 말풍선에 빨간 숫자 배지로 알려줌. 기존 파티 연결(Realtime 채널)을 그대로 재사용해서 별도 서버 저장 없이 구현. 두 브라우저로 협동전·경쟁전 양쪽 모두 실제 주고받기까지 확인함
- 채팅 기능을 테스트하다가 발견한 버그 수정: 협동전·경쟁전 배틀 화면에서 "나가기"/방 코드와 "스테이지·몬스터·마나" 정보 글씨가 휴대폰 좁은 화면에서 겹쳐 보이던 문제. 겹치던 글씨를 각자 전용 줄로 나눠서 해결(화면 위쪽 빈 공간을 활용)
- 버그 제보 기능 추가: 덱 선택 화면의 "버그 제보" 링크를 누르면 내용을 적어 보낼 수 있고, 운영자("관리") 계정의 우편함으로 바로 전달됨. Supabase에 본인 인증된 사용자만 호출 가능한 서버 함수(`submit_bug_report`)를 새로 추가해 처리(우편함의 받는 사람 칸 이름이 `recipient_id`라는 걸 실제로 우편을 보내보고서야 알게 되어, 처음 만든 함수를 한 번 더 고쳐서 완성함)
- 파티 방 만들기 개선: 초대 코드를 자동 생성하지 않고 방장이 직접 정하도록 변경(이미 쓰이는 코드면 만들 때 바로 알려줌). 파티 화면에 "지금 열려 있는 방" 목록이 실시간으로 뜨고, 목록에서 방을 탭하면 코드를 몰라도 바로 참가할 수 있음(방장이 게임을 시작하거나 방을 나가면 목록에서 자동으로 사라짐). 별도 서버 저장 없이 기존 파티 연결 방식(Realtime)과 같은 방식의 공용 로비 채널로 구현
- 7일 출석 체크가 같은 날 여러 번 받아지던 버그 수정: 받은 직후 서버 저장(20초 주기)이 되기 전에 바로 새로고침하거나 창을 닫으면, 다음 접속 때 서버에 남아있던 "아직 안 받음" 상태가 다시 덮어써서 또 받을 수 있었음. 출석 보상을 받는 즉시 서버에 바로 저장하도록 수정해서 해결
- 덱 선택 화면에서 유닛을 더블 탭하면 그 유닛의 도감 정보(이름·등급·분류·특성·공격/속도/사거리) 팝업이 뜨도록 추가. 오른쪽 위 ✕와 "닫기" 버튼으로 닫을 수 있음(한 번 탭은 기존처럼 덱에 넣고 빼는 동작 그대로 유지, 더블 탭 순간에는 선택 상태가 원래대로 되돌아감)
- 탈퇴 후 새로 가입해도 이전 계정 데이터를 이어받던 버그 수정: 탈퇴·로그아웃해도 폰 브라우저에 골드·컬렉션 같은 이전 데이터가 남아있었고, 새 계정은 서버에 데이터가 없어서 그 남은 데이터가 그대로 새 계정에 저장되던 것이 원인. 이제 (1) 새로 가입할 때, (2) 탈퇴할 때 이 브라우저의 게임 데이터를 지우고, (3) 같은 폰에서 다른 계정으로 로그인하면(브라우저에 남은 데이터의 주인이 다르면) 지우고 시작함. 이 수정 이전에 이미 오염된 계정은 자동으로 깨끗해지지 않으므로 탈퇴 후 재가입이 필요함
- 비밀번호 찾기 추가: 가짜 이메일 방식이라 메일로 재설정 링크를 보낼 수 없어서 운영자 처리 방식으로 구현. 로그인 화면 "비밀번호를 잊으셨나요?"에 닉네임을 넣으면 "관리" 우편함에 요청이 도착하고, "관리"가 우편함 오른쪽 위 "비번 초기화"에서 임시 비밀번호를 정해 직접 알려줌. Supabase 서버 함수 `request_password_reset`(로그인 전 호출 가능)과 `admin_reset_password`(운영자만 가능, 일반 유저가 호출하면 거부됨 확인)를 추가. 아직 비밀번호 변경 기능은 없어서 임시 비밀번호를 계속 써야 함
- 비밀번호 변경 기능 추가: 덱 선택 화면의 "비밀번호 변경" 링크(파티 버튼 아래)에서 현재 비밀번호(운영자가 준 임시 비밀번호도 가능) → 새 비밀번호 → 새 비밀번호 확인 순으로 입력해 직접 바꿀 수 있음. 현재 비밀번호가 틀리거나, 확인이 안 맞거나, 6자 미만이면 안내가 뜸. 서버 추가 설정 없이 Supabase 기본 기능으로 처리
- 운영자 전용 "회원 목록" 추가: 운영자("관리") 우편함 왼쪽 위 "회원 목록"에서 운영자를 뺀 모든 가입자 닉네임과 마지막 접속 일시(최근 접속 순)를 볼 수 있음. 마지막 접속은 접속할 때와 접속 중 20초마다 갱신되는 저장 시각(`player_data.updated_at`)과 로그인 시각 중 더 최근 값을 사용(별도 접속 기록 표 없음). 서버 함수 `admin_list_members`는 운영자만 호출 가능(일반 유저 호출 거부 확인)
- 운영자 회원 목록에 "지금 접속 중" 표시 추가: 접속 중인 사람은 🟢와 "접속 중"으로 맨 위에 표시되고, 창을 열어둔 동안 실시간으로 바뀜(그 외에는 마지막 접속 일시). 로그인한 모든 사람이 앱을 켜는 동안 공용 실시간 채널(`global-lobby`)에 들어가 있는 것으로 판단하므로, 이 기능이 들어간 버전을 아직 새로고침하지 않은 사람은 접속 중으로 안 보일 수 있음
- 덱 선택 화면과 개인전 화면 공용 실시간 채팅 추가: 위 공용 채널로 접속 중인 모든 사람이 대화하며, 오른쪽 위 말풍선 버튼으로 열고 닫음(닫힌 사이 온 메시지는 빨간 숫자 배지). 두 화면을 오가도 최근 대화가 이어짐(서버에 저장하지 않아 새로고침하면 사라짐). 파티전 채팅은 기존처럼 별도이며 공용 채팅과 섞이지 않음
- 공용 채팅창 위쪽에 "🟢 접속 중 N명: 닉네임들" 목록 추가(일반 유저도 볼 수 있음, 본인은 "(나)"로 표시, 사람이 들어오고 나가면 실시간으로 바뀜). 접속 중인 사람을 보고 바로 말을 걸어 같이 게임할 수 있게 하기 위함. 가입자 전체 목록과 마지막 접속 일시는 계속 운영자 전용
- 로그인 후 첫 화면을 항상 덱 선택 화면으로 변경(저장된 덱이 있어도 바로 게임이 시작되지 않고, "시작" 버튼을 눌러야 시작됨)
- 유닛 능력 밸런스 조정(`data/units.json`): 사거리를 종류별로 1·2·3칸으로 나눔(기절·처형 1칸 / 광역·둔화·독·다중 사격·사슬 번개·마나 흡수 2칸 / 단일 딜러·방어력 감소·관통·치명 강타 3칸), 버프·냉기 결계는 6칸까지 넓어지던 것을 등급별 2~4칸으로 축소. 공격 속도도 종류별로 0.6(처형)~1.6(마나 흡수)까지 벌림. 사거리·속도가 바뀐 만큼 공격력을 보정해서 초당 피해량은 크게 달라지지 않게 함(사거리 1칸은 공격력 +20%, 3칸은 -10%). 둔화 감속량을 25%~60%로 상향(기존 20~50%)
- 유닛별 상세 설명 추가: 도감 카드에 핵심 수치(예: "이동속도 -25%", "기절 15% · 1초")를 표시하고, 도감 카드를 탭하거나 덱 선택 화면에서 유닛을 더블 탭하면 확률·지속시간·대상 수까지 적힌 상세 설명 팝업이 뜸. 설명 문장은 `core/unitDescription.ts`가 units.json 수치에서 자동으로 만들어서 수치를 바꿔도 설명이 저절로 따라감
- 개인전에서 감속(파랑)·기절(노랑)·독(초록)·방어 감소(빨강) 상태에 걸린 몬스터 주위에 색깔 링을 표시해서 효과가 실제로 적용되는지 눈으로 볼 수 있게 함. 알려진 한계: 협동전·경쟁전에서는 아직 감속·기절 등 특수 효과 없이 기본 공격만 적용됨
- PC 화면 글씨 크기 문제 수정: 카드 폭에 비례해 글씨 크기를 계산하던 도감·컬렉션·연구·덱 선택 화면이 PC처럼 넓은 화면에서는 글씨가 너무 커져서 넘치던 문제를, 폰에서는 지금 크기 그대로 두고 넓은 화면에서만 상한(15px 등)을 두는 방식(`capPx`)으로 해결. PC에서 출석 팝업이 화면 폭만큼 늘어나 깨지던 것, PC 덱 선택 화면 하단 "저장/시작" 버튼이 화면 밖으로 잘리던 것도 함께 수정
- 협동전·경쟁전에도 유닛 특수 효과 적용: 감속·기절·독·방어 감소, 광역·관통·연쇄, 치명타·처형, 다중 사격, 마나 흡수·마나 생성, 버프, 냉기 결계가 개인전과 같은 규칙으로 작동함. 공통 계산은 화면과 무관한 `core/effectsEngine.ts`에 모았고(시뮬레이션으로 효과별 결과 검증), 상태이상에 걸린 몬스터는 색깔 링으로 표시됨. 경쟁전은 각자 자기 필드에서 바로 적용하고, 협동전은 방장이 몬스터를 계산하므로 파티원이 "어떤 유닛으로 어느 몬스터를 쳤는지"(냉기 결계는 사거리 안 몬스터 번호)를 방장에게 보내 방장이 적용하며, 걸린 상태는 몬스터 위치 정보와 함께 파티원 화면에 전달됨. 두 명이 실제로 협동전·경쟁전을 해서 링 표시까지 확인함. 아직 합성·강화·연구 보너스는 파티전에 연결 안 됨
- 협동전·경쟁전에도 개인전의 나머지 시스템 적용: 소환 후 직접 칸을 골라 배치(자동 배치 제거), 유닛 끌어서 이동·같은 유닛끼리 합치기(별), 더블탭 강화, 강화·레벨·연구 배율(경쟁전 균형 모드는 레벨·연구 제외), 사거리 보기/끄기 버튼, 발사체·처치 이펙트. 공통 로직은 `core/playerField.ts`, 연출은 `core/combatVfx.ts`로 분리.
- 몬스터 길·슬롯 배치 변경: 길이 칸 위를 가로지르지 않고 칸 사이 틈(세로 틈 → 가로 틈)으로만 직각으로 꺾여 지나가도록 변경(`toOrthogonalPath`). 칸 사이 틈을 칸 크기의 0.7배(`GAP_RATIO`)로 넓혀 몬스터가 지나가도 유닛이 가려지지 않음. 화면 가장자리를 타던 두 길은 안쪽 지그재그 길로 교체.
